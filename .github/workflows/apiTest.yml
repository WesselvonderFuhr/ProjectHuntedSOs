name: Node.js CI

on:  
  push:
  pull_request:
    branches:
      - development-API

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [12.x]

    steps:
    - run: npm test
